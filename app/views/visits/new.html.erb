<% content_for(:title) { t(".title") } %>

<header>
  <h1><%= t(".title") %></h1>
</header>

<%= form_with model: @presenter.form, url: visits_path do |form| %>
  <%= render partial: "form", locals: { presenter: @presenter, form: form } %>
<% end %>
