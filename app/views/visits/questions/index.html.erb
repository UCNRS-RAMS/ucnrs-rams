<% content_for(:title) { t(".title") } %>
<%= render partial: "visits/steps", locals: { presenter: @presenter } %>
<%= render partial: "visits/questions/questions", locals: { presenter: @presenter } %>
