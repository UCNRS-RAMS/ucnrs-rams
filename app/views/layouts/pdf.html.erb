<!DOCTYPE html>
<html>
  <head>
    <title>RAMS | <%= @page_title || "PDF" %></title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <%= wicked_pdf_stylesheet_link_tag 'application' %>
  </head>
  <body class="<%= body_class %>">
    <%= yield %>
  </body>
</html>
