<div class="manager-navbar lower">
  <div class="container">
    <div class="navbar-content">
      <div class="left-side">
          <%= image_tag layout_presenter.current_reserve_logo,
            alt: t("alt.reserve_logo"), height: "65px", class: "logo" %>

        <small id="title-extra">
          <strong><%= layout_presenter.current_reserve_name %></strong>
          <br />
          <%= layout_presenter.current_reserve_managing_campus_name %>
        </small>
      </div>
      <div class="right-side">
        <ul class="navbar-nav">
          <li>
            <% case layout_presenter.current_user_dashboard %>
            <% when :visits then dashboard_path = manager_reserve_dashboard_visits_path(layout_presenter.current_reserve) %>
            <% when :calendar then dashboard_path = manager_reserve_dashboard_calendar_path(layout_presenter.current_reserve) %>
            <% else %>
              <% dashboard_path = manager_reserve_dashboard_path(layout_presenter.current_reserve) %>
            <% end %>

            <%= link_to t(".link.visits"), dashboard_path,
              class:"nav-link #{active_class_for(["dashboard", "visits"])}" %>
          </li>
          <li>
            <%= link_to t(".link.projects"),
              manager_reserve_projects_path(layout_presenter.current_reserve),
              class:"nav-link #{active_class_for("projects")}"
            %>
          </li>
          <li>
            <%= link_to t(".link.invoices"),
              manager_reserve_invoices_path(layout_presenter.current_reserve),
              class:"nav-link #{active_class_for("invoices")}"
            %>
          </li>
          <li>
            <%= link_to  t(".link.reports"),
              report_part_1_manager_reserve_report_path(layout_presenter.current_reserve, Date.current.year),
              class:"nav-link #{active_class_for("reports")}"
            %>
          </li>
          <li>
            <%= link_to t(".link.reserve_info"),
              edit_manager_reserve_reserve_info_reserve_details_path(layout_presenter.current_reserve),
              class:"nav-link #{active_class_for("reserve_info")}"
            %>
          </li>
          <li>
            <%= link_to t(".link.users"),
              manager_reserve_users_path(layout_presenter.current_reserve),
              class:"nav-link #{active_class_for("users")}"
            %>
          </li>
          <li>
            <%= link_to "https://ramshelp.ucnature.org/managers", id: "help-link", class:"nav-link", target: "_blank" do %>
              <%= t(".link.help") %>
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 22 22" fill="none" stroke="currentColor"
                stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-external-link">
                <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"></path>
                <polyline points="15 3 21 3 21 9"></polyline>
                <line x1="10" y1="14" x2="21" y2="3"></line>
              </svg>
            <% end %>
          </li>
          <li>
              <%= link_to t(".aria.sign_out"), href: destroy_user_session_path,
                title: t(".aria.sign_out"), "aria-label": t(".aria.sign_out"),
                class: ["nav-link", "nav-link-icon"], id: "log-out" do
              %>
                <span id="log-out-icon">
                  <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                    stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-log-out">
                    <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"></path>
                    <polyline points="16 17 21 12 16 7"></polyline>
                    <line x1="21" y1="12" x2="9" y2="12"></line>
                  </svg>
                </span>
              <% end %>
            </li>
          <li>
            <%= link_to t(".aria.my_profile"),
              href: edit_manager_reserve_user_path(layout_presenter.current_reserve, layout_presenter.current_user),
              title: t(".aria.my_profile"), "aria-label": t(".aria.my_profile"),
              class: ["nav-link", "nav-link-icon"] do
            %>
              <%= tag.span layout_presenter.current_user.initials, id: "user-initials" %>
            <% end %>
          </li>
        </ul>

        <%= image_tag "hamburger.svg", alt: t(".alt_text.mobile_menu"),
          class: "navbar-hamburger display-none" %>
      </div>
    </div>
  </div>
</div>
