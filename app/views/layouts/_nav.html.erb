<nav class="main-navigation" role="navigation" style="<%= reserve_hero_image %>">
  <%= render(partial: 'layouts/manager_superbar', locals: { current_user: current_user }) if current_user&.is_manager? %>
  <%= render partial: 'layouts/navbar', locals: { current_user: current_user } %>
</nav>
