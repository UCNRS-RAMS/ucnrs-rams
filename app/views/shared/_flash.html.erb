<turbo-frame id="flash">
  <section role="region" aria-label="errors" class="flash">
    <% flash.each do |type, message| %>
      <%= tag.div message.html_safe, class: type %>
    <% end %>
  </section>
</turbo-frame>
