<div class="card">
  <%= link_to reserve_path(reserve.id) do %>
    <div class="image">
      <%= image_tag reserve.image_placeholder, alt: t(".alt_text.reserve_picture"), class: "reserve-image", height: 165, width: 270 %>
    </div>
    <header><%= reserve.name %></header>
  <% end %>
</div>
