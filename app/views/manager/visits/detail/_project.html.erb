<div class="select_or_new">
  <div>
    <h3><%= t(".project_title") %></h3>
    <%= link_to manager_reserve_project_path(reserve_id: presenter.reserve.id, id: presenter.project.id), target: :_blank, class: "display-link clear-padding" do %>
      <h4><%= presenter.project_title %></h4>
    <% end %>  
  </div>
</div>
