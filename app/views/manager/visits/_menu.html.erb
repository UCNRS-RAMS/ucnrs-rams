<section>
  <ul class="visit-menu-bar">
    <li>
      <%= render partial: "tab",
        locals: @presenter.tab_params(path: manager_reserve_visit_summary_path(visit_id: @presenter.visit.id), classes: "active") %>
    </li>

    <li>
      <%= render partial: "tab",
        locals: @presenter.tab_params(id: "details", path: edit_manager_reserve_visit_detail_path(visit_id: @presenter.id)) %>
    </li>

    <li>
      <%= render partial: "tab",
        locals: @presenter.tab_params(id: "visitors") %>
    </li>

    <li>
      <%= render partial: "tab",
        locals: @presenter.tab_params(id: "reserve_info", path: manager_reserve_visit_reserve_info_index_path(visit_id: @presenter.id)) %>
    </li>

    <li>
      <%= render partial: "tab",
        locals: @presenter.tab_params(id: "policies") %>
    </li>

    <li>
      <%= render partial: "tab",
        locals: @presenter.tab_params(id: "activity") %>
    </li>

    <li>
      <%= render partial: "tab",
        locals: @presenter.tab_params(id: "invoices") %>
    </li>
  </ul>
</section>
