<turbo-frame id="waivers-table">
  <section class="reserve-info-subsection">
    <table class="waivers-table">
      <thead>
        <tr>
          <th><%= t(".name") %></th>
          <th><%= t(".description") %></th>
          <th><%= link_to t(".new"), new_manager_reserve_reserve_info_waiver_path(current_reserve), data: { "turbo-frame": "modal-content" } %></th>
        </tr>
      </thead>
      <tbody>
        <%= render partial: "manager/reserve_info/waivers/waiver", collection: waivers %>
      </tbody>
    </table>
  </section>
</turbo-frame>
