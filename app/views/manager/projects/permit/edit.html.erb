<turbo-frame id="project-details">
  <div id="project-permit-edit">
    <turbo-frame id="permit-questions">
      <%= render partial: "shared/projects/permit_questions", locals: { presenter: @presenter } %>
      <section class="controls">
        <button form="questions" class="active" data-turbo="false">
          <%= t(".save_changes") %>
        </button>
      </section>
    </turbo-frame>
  </div>
</turbo-frame>
